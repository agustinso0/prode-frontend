# Environment

Environment parsing and validation belongs here.

Do not read or interpret environment variables from feature components.

See `docs/frontend-api-env.md` for the Vite environment naming and validation contract.
