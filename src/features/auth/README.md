# Auth

Authentication user experience domain for sign-in, sign-out, and session-facing screens.

Provider integration and session adapters belong in `src/lib/auth`. Route protection belongs in `src/routes`.
