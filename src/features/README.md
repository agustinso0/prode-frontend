# Features

Product domains live here. Each feature owns its pages, domain-specific components, hooks, schemas, stores, API helpers, and tests.

Features must not import from other features. Shared, domain-neutral code belongs in `src/shared`; base integrations belong in `src/lib`.
