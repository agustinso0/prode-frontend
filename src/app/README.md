# App

Application composition lives here: root app component, provider composition, and global shell concerns.

Product screens do not live here. Route-targeted pages belong in `src/features/<domain>/pages` and are connected through `src/routes`.
